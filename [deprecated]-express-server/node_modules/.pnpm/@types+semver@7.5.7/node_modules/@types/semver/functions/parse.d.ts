import SemVer = require(".pnpm/@types+semver@7.5.7/node_modules/@types/semver/classes/semver");
import semver = require(".pnpm/@types+semver@7.5.7/node_modules/@types/semver");

/**
 * Return the parsed version as a SemVer object, or null if it's not valid.
 */
declare function parse(
    version: string | SemVer | null | undefined,
    optionsOrLoose?: boolean | semver.Options,
): SemVer | null;

export = parse;
