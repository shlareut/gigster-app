import { RuleOptionConnection } from "../rules/check-sql.rule";
import { ConnectionPayload } from "../rules/check-sql.utils";
export declare function createConnectionManager(): {
    getOrCreate: (databaseUrl: string) => ConnectionPayload;
    close: (params: CloseConnectionParams) => void;
};
export interface CloseConnectionParams {
    connection: RuleOptionConnection;
    projectDir: string;
}
